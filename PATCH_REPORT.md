# AI手术视频专题增量补丁报告

本补丁直接扩展原肝胆胰资源库的数据层、生成器、页面和既有 GitHub Actions 工作流。没有建立第二个网站。

## 数据与逐条核验

输入的45条合并资源与1条待核线索全部处理，原始61行目录通过带来源命名空间的映射保留。复核后为30篇论文/研究、9个数据集、1个审计工具、1个模型卡、4个研究演示入口，另1条待核线索。UniSurgSAM已找到预印本，因此原项目记录保留ID、更新为论文类型。不是45篇LLM论文。

各条当前检查、原链接和重定向终点、正文阅读范围、实际任务/术式、开放条件、来源依据、限制和历史修订见 `data/ai_records.json` 与 `data/reports/ai-verification.json`。未取得正文的条目标为摘要/题录范围；代码存在不代表复现成功；4个演示均未测试播放、完整观看或嵌入，仅提供原站入口。

重点修订包括SAM2S→SurgSLOT、SurgiSAM2与Surgical SAM 2区分、SurgVLM论文与仓库版本漂移、Surgical SAM 3.1模型卡更新、SurgVISTA作者更正、D2GPLand期刊扩展、Endoscapes配套技术报告身份与计数差异。没有把结构标签当成原始手术类型。

第二次实际导入新增0、修订0，46条保持不变。原临床368条记录文件与补丁前备份逐字节相同；浏览器个人存储键保持不变。原临床和AI论文无重复DOI。详见 `data/reports/ai-import.json`、`ai-preservation.json`。

## 界面与更新

新增8个可折叠专题分区、3个器官的相关AI入口、临床/AI/全部范围切换、别名检索、多条件筛选和稳定锚点。AI研究演示与临床视频分开计数。论文卡片包含具体方法/数据单位/提示条件/适用范围笔记和来源，人工补充笔记继续仅本地保存。

原周一北京时间09:17排期不变。原 `scripts/update.py` 调用 `ai_update.py`，复用累计状态、检查点、发布校验和release-good回滚机制。日常浏览及常规元数据更新不调用大模型，不需要模型API密钥。

Europe PMC/PubMed/arXiv使用器官×任务组×模型组查询，不强制LLM；Crossref按已核DOI监视出版和更正元数据；已知arXiv标识单独追踪版本；作者代码、数据、模型和项目链接以限量文本请求巡检。无模型执行、GPU、数据/权重/视频下载。

首次真实宽检索后增加视频/腹腔镜/机器人限定，旧查询记录保留。未完成来源的原查询窗口与分页检查点持久化，由云端继续运行。失败不推进该来源成功进度；新候选和变化进入待核区，不覆盖人工科学解读。检索覆盖不等于穷尽，MICCAI/CVF全站历史目录等仍明确列为人工补查缺口。

## 验收与限制

本地99项Python及15项JavaScript测试通过，覆盖重复导入、命名空间、强标识冲突、共享仓库不同实体、跨来源去重、科学笔记保留、查询失败与数月后恢复、分页未完成、二进制不下载、安全渲染、累计数据缺失拒绝等。

实际浏览器HTTP测试通过临床默认视图、AI切换、SAM2S别名、组合筛选、详细笔记展开、收藏刷新保留后恢复测试状态、命名空间旧锚点。390px窄屏document宽度与scrollWidth同为390，无新增脚本错误。

实际file://请求被浏览器工具URL安全策略拒绝，未绕过，不能报告该项通过。HTML仍自包含并保留双击入口，Origin-null云端数据校验由发布检查另行验证。浏览器与运行证据见 `data/reports/ai-browser-tests.json` 和 `data/runs/ai-*.json`。

## 部署阶段

代码已完成。本地程序和HTTP浏览器验证已完成，file://实测受工具策略阻塞。原云端站点及周调度已存在；本补丁云端发布和手动实际运行结果将记录在 `data/reports/ai-cloud-acceptance.json`。当前不能将本补丁工作流文件存在等同于已完成部署，也不能将手动运行称作真实定时触发。

## 文件与回滚

核心新增：`scripts/ai_import.py`、`ai_update.py`、`ai_payload.py`、`src/ai.js`、`config/ai_sources.json`、`ai_taxonomy.json`、6个独立AI累计数据文件、AI测试与报告。已有生成器、app.js、HTML模板、样式、白名单、恢复程序和原周工作流作兼容扩展。

可恢复备份在本地 `work/hpb/ai-patch-20260915/before-patch/`，记录补丁前远端main为 `59c5d85788316aed4d59879ce7e5acee2ccb9b0a`。回滚前先备份当前累计数据与个人笔记：恢复旧生成源和临床数据后重新生成；保留AI数据与其历史文件供后续恢复。云端发布故障自动恢复release-good已核制品，不清空main累计数据。若需撤销本补丁，针对补丁提交生成反向变更，不强制重置分支或丢弃后来更新。
